#!/usr/bin/env python

"""This file only exists to enable editable installation with 'pip install -e'.

All configuration goes into the setup.cfg file.
"""

import setuptools

if __name__ == "__main__":
    setuptools.setup()
